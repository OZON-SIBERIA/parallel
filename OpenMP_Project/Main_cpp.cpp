#include <stdio.h>
#include <thread>
#include <stdio.h>
#include <chrono>
#include <mutex>
#include <vector>
#include <omp.h>

double f(double x)
{
	return 4 / (1 + x * x);

int main(int argc, char** argv)
{
	double integral_CPP(double a, double b);
	return 0;

}